Legacy image location kept for backwards compatibility.

For a new client, copy `assets/brands/straight-razors/` to a new folder,
replace the standard files, and change `brand.assetsDir` in `config.js`.
